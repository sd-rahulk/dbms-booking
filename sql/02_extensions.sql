-- Physical extensions for application functionality.
PRAGMA foreign_keys = ON;
CREATE TABLE IF NOT EXISTS auth_account (account_id TEXT PRIMARY KEY, email TEXT NOT NULL UNIQUE, password_hash TEXT NOT NULL, role TEXT NOT NULL CHECK (role IN ('PASSENGER','DRIVER','ADMIN')), user_id TEXT UNIQUE REFERENCES app_user(user_id) ON DELETE CASCADE, driver_id TEXT UNIQUE REFERENCES driver(driver_id) ON DELETE CASCADE, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS trip_event (event_id TEXT PRIMARY KEY, trip_id TEXT NOT NULL REFERENCES trip(trip_id) ON DELETE CASCADE, actor_id TEXT REFERENCES app_user(user_id) ON DELETE SET NULL, event_type TEXT NOT NULL, previous_status TEXT, new_status TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
ALTER TABLE driver ADD COLUMN availability_status TEXT NOT NULL DEFAULT 'OFFLINE' CHECK (availability_status IN ('ONLINE','OFFLINE','ON_TRIP'));
ALTER TABLE driver ADD COLUMN verification_status TEXT NOT NULL DEFAULT 'PENDING' CHECK (verification_status IN ('PENDING','VERIFIED','REJECTED'));
ALTER TABLE driver ADD COLUMN is_active INTEGER NOT NULL DEFAULT 1;
ALTER TABLE vehicle ADD COLUMN registration_number TEXT;
ALTER TABLE vehicle ADD COLUMN is_active INTEGER NOT NULL DEFAULT 1;
ALTER TABLE vehicle_document ADD COLUMN document_type TEXT NOT NULL DEFAULT 'RC';
ALTER TABLE vehicle_document ADD COLUMN document_url TEXT;
ALTER TABLE trip ADD COLUMN status TEXT NOT NULL DEFAULT 'REQUESTED' CHECK (status IN ('REQUESTED','ASSIGNED','ACCEPTED','IN_PROGRESS','COMPLETED','CANCELLED'));
ALTER TABLE trip ADD COLUMN distance_km REAL;
ALTER TABLE trip ADD COLUMN estimated_fare REAL;
ALTER TABLE trip ADD COLUMN final_fare REAL;
ALTER TABLE trip ADD COLUMN requested_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE trip ADD COLUMN started_at TEXT;
ALTER TABLE trip ADD COLUMN completed_at TEXT;
ALTER TABLE trip ADD COLUMN cancelled_at TEXT;
ALTER TABLE trip ADD COLUMN cancellation_reason TEXT;
ALTER TABLE payment ADD COLUMN status TEXT NOT NULL DEFAULT 'PENDING' CHECK (status IN ('PENDING','PAID','FAILED','REFUNDED'));
ALTER TABLE payment ADD COLUMN transaction_reference TEXT;
ALTER TABLE payment ADD COLUMN created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP;
