export function StatusPill({ status }: { status: string }) { const tone = status === "COMPLETED" || status === "PAID" || status === "VERIFIED" ? "bg-[#e3eee1] text-[#3e7040]" : status === "CANCELLED" || status === "FAILED" || status === "REJECTED" ? "bg-[#f5e1dd] text-[#8b3528]" : status === "IN_PROGRESS" ? "bg-amber/20 text-[#88611d]" : "bg-paper text-ash"; return <span className={`inline-flex px-2.5 py-1 text-[10px] font-bold uppercase tracking-[.12em] ${tone}`}>{status.replaceAll("_", " ")}</span>; }
