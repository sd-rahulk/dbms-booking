"use client";
import { ShieldCheck, UserRound } from "lucide-react";
export function ProfileClient({ session }: { session: any }) { return <div className="mx-auto max-w-5xl px-5 py-8 sm:px-8 lg:px-12 lg:py-12"><header className="border-b border-line pb-8"><p className="eyebrow">Account</p><h1 className="display mt-3 text-5xl tracking-[-.05em]">Your profile<span className="text-amber">.</span></h1></header><div className="mt-8 grid gap-5 md:grid-cols-2"><div className="border border-line bg-[#fbfaf7] p-7"><UserRound size={20} /><p className="eyebrow mt-10">Signed in as</p><p className="mt-2 text-lg font-semibold">{session?.email}</p><p className="mt-2 text-sm text-ash">{session?.role?.toLowerCase()} account · {session?.userId ?? session?.driverId ?? "admin"}</p></div><div className="bg-amber p-7"><ShieldCheck size={20} /><h2 className="display mt-10 text-3xl">Your data,<br />kept connected.</h2><p className="mt-4 max-w-sm text-sm leading-6">AERIDE stores operational records and audit events, never card numbers, PINs, or sensitive payment credentials.</p></div></div></div>; }
